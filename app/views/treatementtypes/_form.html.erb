<%= form_with(model: treatementtype) do |form| %>
  <% if treatementtype.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(treatementtype.errors.count, "error") %> prohibited this treatementtype from being saved:</h2>

      <ul>
        <% treatementtype.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name, class:"form-control" %>
  </div>
   <br>
  <div>
    <%= form.submit "Treatment Type", class:'btn btn-secondary' %>
  </div>
<% end %>
